export const igContacts: never[] = [];

export const igConversations: Record<string, { from: 'customer' | 'agent' | 'ai'; text: string; time: string; aiGenerated?: boolean; mediaType?: 'image' | 'reel' | 'story' }[]> = {};

export const igPosts: never[] = [];

export const igStats = {
  followers: '0',
  following: 0,
  totalDMs: 0,
  activeDMs: 0,
  avgResponseTime: '—',
  aiHandled: '0%',
  conversionRate: '0%',
  profileVisits: 0,
};

export const igCampaigns: never[] = [];
